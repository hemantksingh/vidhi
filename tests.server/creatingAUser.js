var should = require('should');

describe("Creating a user with valid values", function() {
	var a;	

	beforeEach(function() {
		a = true; 
	})

	it("should pass", function(){
		a.should.equal(true);
	})
})
